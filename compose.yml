services:
  jenkinsgraphql-generator:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: jenkinsgraphql-generator
    restart: unless-stopped
    ports:
      - "3333:3000"
    environment:
      NODE_ENV: production
      PORT: 3000
      HOSTNAME: 0.0.0.0
