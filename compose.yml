services:
  graphql-burp-web:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: graphql-burp-web
    restart: unless-stopped
    ports:
      - "3333:3000"
    environment:
      NODE_ENV: production
      PORT: 3000
      HOSTNAME: 0.0.0.0
